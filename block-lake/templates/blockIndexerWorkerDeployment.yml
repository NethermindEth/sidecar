apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "blockIndexerWorkerName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "blockIndexerWorkerName" . }}
spec:
  selector:
    matchLabels:
      app: {{ template "blockIndexerWorkerName" . }}
{{ toYaml .Values.workers.blockIndexer.updateStrategy | indent 2 }}
  template:
    metadata:
      labels:
        app: {{ template "blockIndexerWorkerName" . }}
        tags.datadoghq.com/env: development
        tags.datadoghq.com/version: "0.0.1"
        tags.datadoghq.com/service: {{ template "blockIndexerWorkerName" . }}
        admission.datadoghq.com/enabled: "true"
    spec:
      serviceAccountName: "blocklake-pods"
      containers:
        - name: {{ template "blockIndexerWorkerName" . }}
          image: {{ .Values.common.image.name }}:{{ .Values.common.image.tag }}
          imagePullPolicy: Always
          command:
            - "/build/bin/cmd/workers/backfillBlockIndexer"
          env:
          {{- $configName := .Values.common.config.name -}}
          {{- range $k, $v := .Values.common.config.values }}
            - name: {{ template "formatEnvVarName" $k }}
              valueFrom:
                configMapKeyRef:
                  key: {{ $k }}
                  name: {{ $configName}}
          {{- end -}}
          {{- $secretName := .Values.common.secret.name -}}
          {{- range $k, $v := .Values.common.secret.values }}
            - name: {{ template "formatEnvVarName" $k }}
              valueFrom:
                secretKeyRef:
                  key: {{ $k }}
                  name: {{ $secretName}}
          {{- end -}}
{{- if .Values.workers.blockIndexer.resources }}
          resources:
{{ toYaml .Values.workers.blockIndexer.resources | indent 12 }}
{{- end}}
