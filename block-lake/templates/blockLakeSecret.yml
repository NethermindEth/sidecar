apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.common.secret.name }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
{{- range $key, $value := .Values.common.secret.values }}
  {{ $key }}: {{ $value | b64enc }}
{{- end }}
