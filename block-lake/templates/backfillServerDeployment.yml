apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "backfillServerName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "backfillServerName" . }}
spec:
  selector:
    matchLabels:
      app: {{ template "backfillServerName" . }}
{{ toYaml .Values.backfillServer.updateStrategy | indent 2 }}
  template:
    metadata:
      labels:
        app: {{ template "backfillServerName" . }}
        tags.datadoghq.com/env: development
        tags.datadoghq.com/version: "0.0.1"
        tags.datadoghq.com/service: {{ template "backfillServerName" . }}
        admission.datadoghq.com/enabled: "true"
    spec:
      serviceAccountName: "blocklake-pods"
      containers:
        - name: {{ template "backfillServerName" . }}
          image: {{ .Values.common.image.name }}:{{ .Values.common.image.tag }}
          imagePullPolicy: Always
          command:
            - "/build/bin/cmd/server"
          env:
          {{- $configName := .Values.common.config.name -}}
          {{- range $k, $v := .Values.common.config.values }}
            - name: {{ template "formatEnvVarName" $k }}
              valueFrom:
                configMapKeyRef:
                  key: {{ $k }}
                  name: {{ $configName}}
          {{- end -}}
          {{- $secretName := .Values.common.secret.name -}}
          {{- range $k, $v := .Values.common.secret.values }}
            - name: {{ template "formatEnvVarName" $k }}
              valueFrom:
                secretKeyRef:
                  key: {{ $k }}
                  name: {{ $secretName}}
          {{- end -}}
{{- if .Values.backfillServer.resources }}
          resources:
{{ toYaml .Values.backfillServer.resources | indent 12 }}
{{- end}}
