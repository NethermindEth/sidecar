apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "transactionLogIndexerWorkerName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "transactionLogIndexerWorkerName" . }}
spec:
  selector:
    matchLabels:
      app: {{ template "transactionLogIndexerWorkerName" . }}
{{ toYaml .Values.workers.transactionLogIndexer.updateStrategy | indent 2 }}
  template:
    metadata:
      labels:
        app: {{ template "transactionLogIndexerWorkerName" . }}
        tags.datadoghq.com/env: development
        tags.datadoghq.com/version: "0.0.1"
        tags.datadoghq.com/service: {{ template "transactionLogIndexerWorkerName" . }}
        admission.datadoghq.com/enabled: "true"
    spec:
      serviceAccountName: "blocklake-pods"
      containers:
        - name: {{ template "transactionLogIndexerWorkerName" . }}
          image: {{ .Values.common.image.name }}:{{ .Values.common.image.tag }}
          imagePullPolicy: Always
          command:
            - "/build/bin/cmd/workers/transactionLogIndexer"
          env:
          {{- $configName := .Values.common.config.name -}}
          {{- range $k, $v := .Values.common.config.values }}
            - name: {{ template "formatEnvVarName" $k }}
              valueFrom:
                configMapKeyRef:
                  key: {{ $k }}
                  name: {{ $configName}}
          {{- end -}}
          {{- $secretName := .Values.common.secret.name -}}
          {{- range $k, $v := .Values.common.secret.values }}
            - name: {{ template "formatEnvVarName" $k }}
              valueFrom:
                secretKeyRef:
                  key: {{ $k }}
                  name: {{ $secretName}}
          {{- end -}}
{{- if .Values.workers.transactionLogIndexer.resources }}
          resources:
{{ toYaml .Values.workers.transactionLogIndexer.resources | indent 12 }}
{{- end}}
